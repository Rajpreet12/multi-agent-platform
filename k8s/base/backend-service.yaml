apiVersion: v1
kind: Service
metadata:
  name: multi-agent-backend-service
  namespace: multi-agent
spec:
  selector:
    app: multi-agent-backend
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP